rules_version = '2';

// By default, deny all reads and writes
// This is a secure starting point for your database rules
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
